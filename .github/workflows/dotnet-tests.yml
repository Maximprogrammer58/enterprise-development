name: .NET Tests

on:
  push:
    branches: [ main, lab_1 ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Restore dependencies
        run: dotnet restore ./AirlineApp.sln

      - name: Build
        run: dotnet build ./AirlineApp.sln --no-restore

      - name: Test
        run: dotnet test ./AirlineApp.Tests/AirlineApp.Tests.csproj --no-build --verbosity normal
